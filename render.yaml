services:
  - type: web
    name: maritime-router
    runtime: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --log-level info
    plan: free
    healthCheckPath: /health
